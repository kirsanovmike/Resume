export interface MenuItem {
  id: string,
  name: string
}

export interface LanguageItem {
  id: string,
  name: string
}

export interface AboutItem {
  id: string,
  name: string,
  buttonText: string,
  position: string,
  textAbout: string,
}